# Machine Learning & Modeling
scikit-learn
pandas
numpy
xgboost
lightgbm
matplotlib
seaborn

# Model Interpretation
shap

deltalake
hops-deltalake
# Delta support required by Hopsworks time travel format
delta-spark
pyarrow
hopsworks
# Web Dashboard & API
flask
gunicorn

# APIs & Utilities
requests
python-dotenv

# hopsworks — its dependency `hsfs>=3.7.0` is NOT on public PyPI.
# Install separately in a Python 3.8–3.12 env if Hopsworks upload is needed:
#   pip install hopsworks==3.7.0
# The upload_to_hopsworks.py script falls back to CSV export when unavailable.
