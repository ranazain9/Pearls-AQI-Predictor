# 🌫️ Pearls AQI Predictor

> AI-powered Air Quality Index forecasting for **Lahore, Punjab, Pakistan** — 3-day predictions using a fully serverless ML pipeline.

---

## 📁 Project Structure

```
Pearls-AQI-Predictor/
│
├── notebooks/                  ← 📓 Exploration & model experiments
│   └── model.ipynb             ← EDA + multi-model training + SHAP analysis
│ 
├── aqi/                        ← 🚀 Production pipeline & dashboard    (optional-pipeline)
│   ├── pipeline.py             ← Main ML pipeline (run this first!)
│   ├── app.py                  ← Flask server  →  http://127.0.0.1:5000
│   └── frontend.html           ← Dashboard UI (HTML + Chart.js)
│
├── src/                        ← 🧠 Core library modules
│   ├── config.py               ← Shared config (paths, feature names)
│   ├── alerts.py               ← Hazardous AQI alert logic
│   ├── features/
│   │   ├── engineering.py      ← Feature computation (lags, rolling, etc.)
│   │   └── fetch_raw.py        ← Open-Meteo API data fetchers
│   ├── training/
│   │   └── train.py            ← Multi-model trainer (RF + Ridge + TF)
│   └── inference/
│       └── predict.py          ← Loads best model, runs 72-hour forecast
│
├── pipelines/                  ← ⚙️ CLI entry points for CI/CD
│   ├── feature_pipeline.py     ← Hourly feature fetch + Hopsworks upload
│   └── training_pipeline.py    ← Daily retrain + Hopsworks register
│
├── app/                        ← 📊 Alternative frontends
│   ├── Frontend.py             ← Frontend dashboard
│   ├── flask_api.py            ← Standalone Flask REST API
│
├── data/                       ← 📂 Data files (large CSVs git-ignored)
│   ├── lahore_aqi_historical_2024_onwards.csv   ← Training data
│   ├── aqi_dashboard_data.json                  ← ⬅ Generated by pipeline.py
│   └── lahore_aqi_3day_forecast.csv             ← ⬅ Generated by pipeline.py
│
├── models/                     ← 🤖 Saved model artifacts (git-ignored)
│   ├── best_model.pkl / .keras ← Best trained model
│   ├── scaler.pkl              ← Fitted StandardScaler
│   └── training_results.json   ← Metrics for all evaluated models
│
├── .github/workflows/          ← 🔄 GitHub Actions CI/CD
│   ├── feature_pipeline.yml    ← Runs every hour (fetches data)
│   └── training_pipeline.yml   ← Runs every day (retrains model)
│
├── requirements.txt            ← Python dependencies
├── .env                        ← Your API keys — NEVER commit this!
└── .env.example                ← Template for .env setup
```

---

## 🚀 Quick Start

### 1. Install dependencies
```bash
pip install -r requirements.txt
pip install -r requirements/requirements.txt
```

### 2. Set up environment variables
```bash
cp .env.example .env
# Edit .env and add your HOPSWORKS_API_KEY if using Hopsworks
```

### 3. Run the ML pipeline
```bash
python pipeline/feature_pipeline.py --upload
python pipeline/training_pipeline.py --register
```
This fetches live weather + air quality data, trains 3 models (Random Forest, Ridge, TensorFlow), picks the best one by RMSE, and exports `data/aqi_dashboard_data.json`.

### 4. Launch the Dashboard
We offer a premium, interactive Streamlit dashboard. Ensure the Flask API is running in the background for live data:

```bash
# Terminal 1: Run the backend API
python app/flask_api.py

# Terminal 2: Run the frontend
streamlit run app/streamlit_app.py
```
Open **http://localhost:8501** in your browser.

---

## 🧪 Jupyter Notebook (Exploration)

Open `notebooks/model.ipynb` to explore:
- Exploratory Data Analysis (EDA) with seasonal trend plots
- Correlation heatmaps across all weather features
- Manual multi-model training and head-to-head RMSE comparison
- SHAP feature importance waterfall charts
- Residual analysis plots

```bash
jupyter notebook notebooks/model.ipynb
```

---

## 📊 Frontends & API Endpoints

### Option A — `app/streamlit_app.py` (Premium UI - Recommended)
```bash
streamlit run app/streamlit_app.py
```
> A stunning, responsive, dark-theme Streamlit dashboard featuring live AQI metrics, dynamic 72-hour forecast charts, and interactive SHAP tabs to explain model predictions for each horizon.

### Option B — `app/flask_api.py` (live inference via `src/` pipeline)    (main-file)
```bash
python app/flask_api.py   # serves at http://127.0.0.1:5000
```
> Uses the already-trained model saved in `models/` to run live inference on every request. Falls back to `data/aqi_dashboard_data.json` automatically if no model file is found.

| Endpoint | Description |
|----------|-------------|
| `GET /` | Same dashboard UI |
| `GET /api/data` | Live dashboard JSON (runs model inference) |
| `GET /api/forecast?days=3` | Live 72h forecast |
| `GET /api/current` | Current AQI + alert status |
| `GET /api/metrics` | Training RMSE for all evaluated models |
| `GET /health` | Health check |

---

## 🔄 CI/CD Automation (GitHub Actions)

| Workflow | Schedule | What it does |
|----------|----------|--------------|
| `feature_pipeline.yml` | Every hour | Fetches latest air quality data, uploads to Hopsworks Feature Store |
| `training_pipeline.yml` | Every day | Retrains all models, selects best, registers to Hopsworks Model Registry |

---

## 🌐 Deployment
This project is production-ready and configured for serverless deployments:
- **Frontend (Streamlit)**: Easily deployable to [Hugging Face Spaces](https://huggingface.co/spaces) or [Streamlit Community Cloud](https://share.streamlit.io/).
- **Backend (Flask API)**: Configured for deployment on [Vercel](https://vercel.com/) via the included `vercel.json`.

---

## 🛠 Technology Stack

| Component | Technology |
|-----------|------------|
| Data APIs | Open-Meteo (free, no key needed) |
| Feature Store | Hopsworks |
| ML Models | Random Forest, Ridge Regression, TensorFlow/Keras |
| Explainability | SHAP |
| CI/CD | GitHub Actions |
| Frontend | Streamlit (`app/streamlit_app.py`) |
| Backend | Flask (`app/flask_api.py`) |
